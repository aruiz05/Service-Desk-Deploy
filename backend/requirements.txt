# Web API framework.
fastapi>=0.116,<1.0

# ASGI server used to run the FastAPI app locally.
uvicorn[standard]>=0.35,<1.0

# Database toolkit used for the SQLite connection.
sqlalchemy>=2.0,<3.0

# PostgreSQL driver for deployed SQLAlchemy connections.
psycopg[binary]>=3.2,<4.0

# Data validation library used by FastAPI.
pydantic>=2.0,<3.0

# Email validation support for Pydantic EmailStr fields.
email-validator>=2.0,<3.0

# Signed admin access tokens.
PyJWT>=2.10,<3.0

# Password hashing for the single admin credential.
bcrypt>=4.0,<5.0
